import TourliDestinations from "../../components/tourli-destinations";

// Renders the Destinations directory on the tags index (/tags). The default tag
// cloud / sort controls are hidden via SCSS so this reads as the "Destinations"
// page; admin tag management still lives under Admin and individual non-destination
// tags remain reachable directly (e.g. /tag/bug) and via search.
<template><TourliDestinations /></template>
