// "Tourli Community" block at the top of the sidebar, rendered into the
// before-sidebar-sections outlet. Template-only (no state).
const TourliSidebarHeader = <template>
  <div class="tourli-sidebar-header">
    <div class="tourli-sidebar-header__title">Tourli Community</div>
  </div>
</template>;

export default TourliSidebarHeader;
